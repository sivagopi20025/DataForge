# Docker Notes

Docker infrastructure for Phase 1 is defined at the repository root in
`docker-compose.yml`.

Current services:

- PostgreSQL 16
- pgAdmin

Application containerization is intentionally deferred until the backend API
deployment shape is finalized.
